when@dev:
    web_profiler:
        toolbar: false            # Disable toolbar to prevent PHP-CGI crashes
    framework:
        profiler:
            collect_serializer_data: false   # Reduce profiler overhead

when@test:
    framework:
        profiler:
            collect: false
            collect_serializer_data: true    # Keep serializer data collection for tests
